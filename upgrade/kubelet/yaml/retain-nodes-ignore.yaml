apiVersion: clusterop.isim.dev/v1alpha1
kind: KubeletUpgrade
metadata:
  name: retain-nodes-ignore
spec:
  failurePolicy: strict
  maxUnavailable: 1
  schedule: "0 0 * * 0"
  selector:
    matchLabels:
      clusterop.isim.dev/kubelet-upgrade: retain-nodes-ignore
  strategy: replace
