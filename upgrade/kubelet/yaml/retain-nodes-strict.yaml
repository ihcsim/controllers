apiVersion: clusterop.isim.dev/v1alpha1
kind: KubeletUpgrade
metadata:
  name: retain-nodes-strict
spec:
  failurePolicy: strict
  maxUnavailable: 1
  schedule: "@weekly"
  selector:
    matchLabels:
      clusterop.isim.dev/kubelet-upgrade: retain-nodes-strict
  strategy: retain
