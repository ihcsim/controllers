apiVersion: clusterop.isim.dev/v1alpha1
kind: KubeletUpgrade
metadata:
  name: replace-nodes-ignore
spec:
  failurePolicy: ignore
  maxUnavailable: 1
  schedule: "@weekly"
  selector:
    matchLabels:
      clusterop.isim.dev/kubelet-upgrade: replace-nodes-ignore
  strategy: replace
